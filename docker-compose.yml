version: "3.7"
services:
  db:
    image: postgres:11
    container_name: db_app
    volumes:
      - ./data:/app/data
    env_file:
      - .env-docker

  app:
    build:
      context: .
    env_file:
      - .env-docker
    container_name: fastapi_app
    command: ["/app/docker/app.sh"]
    ports:
      - 9991:8000
    depends_on:
      - db



